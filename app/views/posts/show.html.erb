<%= render partial: "post", object: @post %>

<%= link_to "Post Index", posts_path %>

<div>
  <br/>
  <%= link_to "Edit Post", posts_edit_path  %>
</div>

<div>
  <br/>
  <%= link_to 'Delete Post', post_path(@post),
              method: :delete, data: { confirm: 'Are you sure?' } %>
</div>
